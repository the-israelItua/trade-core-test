import styled from "styled-components";

const ControlWrapper = styled.div`
  position: absolute;
  display: flex;
  right: 1rem;
  bottom: 1rem;

  button {
    margin-left: 1rem;
  }
`;

export { ControlWrapper };
